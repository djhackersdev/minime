export interface Settings {
  bgMusic: number;
  forceQuitEn: boolean;
  steeringForce: number;
  bgVolume: number;
  seVolume: number;
  cornerGuide: boolean;
  lineGuide: boolean;
  ghostEn: boolean;
  taResultSkip: boolean; // TA = Time Attack presumably
}
